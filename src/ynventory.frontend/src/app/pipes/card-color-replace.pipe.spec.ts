import { CardColorReplacePipe } from './card-color-replace.pipe';

describe('CardColorReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new CardColorReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
